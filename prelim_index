<?php
session_start();
require_once('connection.php');

$newconnection = new Connection();

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    if (isset($_POST['addproduct'])) {
        $newconnection->addProduct(); 
    } 
    elseif (isset($_POST['delete_product'])) {
        $newconnection->deleteProduct(); 
    } 
    // elseif (isset($_POST['edit_student'])) {
    //     $student = $newconnection->getStudentById($_POST['edit_id']); 
    // } elseif (isset($_POST['update_student'])) {
    //     $newconnection->updateStudent(); 
    // }
}
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD FOR PRODUCTS</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <style>
        body{
            background-color: darkgray;
        }
    </style>
</head>

<body>
<div class="container">
    <form class="row" action="" method="POST">
        <div class="row mt-3">
            <!-- Row 1 -->
            <div class="col-md-4">
                <label for="product_name" class="form-label">Product Name</label>
                <input type="text" name="productname" class="form-control" id="product_name" required>
            </div>
            <div class="col-md-4">
                <label for="category" class="form-label">Category</label>
                <select id="category" name="category" class="form-select" required>
                    <option selected>Food</option>
                    <option>Drinks</option>
                    <option>Clothes</option>
                </select>
            </div>
            <div class="col-md-4">
                <label for="quantity" class="form-label">Quantity</label>
                <input type="text" name="quantity" class="form-control" id="quantity" required>
            </div>
        </div>  

        <div class="row mt-3">
            <!-- Row 2 -->
            <div class="col-md-4">
                <label for="datepurchase" class="form-label">Date Purchase</label>
                <input type="date" name="datepurchase" class="form-control" id="datepurchase" required>
            </div>
        </div>

        <div class="col-10">
            <button type="submit" class="btn btn-primary float-end" name="addproduct">ADD</button>
        </div>

        <div class="col-md-4 ms-auto mt-3"> <!-- Added ms-auto to move it to the right -->
            <input class="form-control" aria-autocomplete="both" id="search-input" 
            autocomplete="off" placeholder="Search" maxlength="64" type="search">
        </div>
    </form>

    <?php if (isset($_SESSION['message'])): ?>
        <h5 class="alert alert-success"> <?= $_SESSION['message']; ?> </h5>
        <?php unset($_SESSION['message']);  ?>  
    <?php endif; ?>
    <!-- Clear message after displaying -->

    <div class="info-table container mt-4">
        <table class="table table-dark table-striped">
            <thead>
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Product Name</th>
                    <th scope="col">Category</th>
                    <th scope="col">Quantity</th>
                    <th scope="col">Date Purchase</th>
                    <th scope="col">    </th>
                </tr>
            </thead>
            <tbody>
            <?php
            $connection = $newconnection->openConnection();
            $stmt = $connection->prepare("SELECT * FROM product_table");
            $stmt->execute();
            $result = $stmt->fetchAll();

            if ($result) {
                foreach ($result as $row) {
                    ?>
                    <tr>
                        <td><?php echo $row->id; ?></td>
                        <td><?php echo $row->Product_Name; ?></td>
                        <td><?php echo $row->Category; ?></td>
                        <td><?php echo $row->Quantity; ?></td>
                        <td><?php echo $row->Date_Purchase; ?></td>
                        <td>
                            <form action="" method="post" class="d-inline">
                                <input type="hidden" name="edit_id" value="<?= $row->id; ?>">
                                <button type="submit" class="btn btn-warning" name="edit_student">Edit</button>
                            </form>
                            <form action="" method="post" class="d-inline">
                                <input type="hidden" name="delete_product" value="<?php echo $row->id; ?>">
                                <button type="submit" class="btn btn-danger" name="delete_product">Delete</button>
                            </form>
                        </td>
                    </tr>
                    <?php
                }
            }
            ?>
            </tbody>
        </table>
    </div>
</div>

</body>
</html>
